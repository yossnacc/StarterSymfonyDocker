<nav class="nav flex-column">
    <a class="brand nav-link font-weight-bold text-white py-3" href="{{ path('back_index') }}">
        <i class="fab fa-angellist"></i> <span>Symfony</span>
    </a>
    <a class="nav-link" href="{{ path('back_index') }}"><i class="fas fa-chart-bar"></i> <span>{{ 'Dashboard' }}</span></a>
    <div class="accordion" id="accordionExample">
        <button class="btn btn-link" type="button" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
            <i class="fas fa-list-ul"></i> <span>{{ 'Accordion' }}</span>
        </button>

        <div id="collapseOne" class="collapse" aria-labelledby="headingOne" data-parent="#accordionExample">
            <a class="nav-link" href="#"><i class="fas">1</i><span>{{ 'Link 1' }}</span></a>
            <a class="nav-link" href="#"><i class="fas">2</i><span>{{ 'Link 2' }}</span></a>
        </div>
    </div>
    <a class="nav-link" href="{{ path('back_user_index') }}"><i class="fas fa-users"></i> <span>{{ 'user.plurial'|trans }}</span></a>
</nav>